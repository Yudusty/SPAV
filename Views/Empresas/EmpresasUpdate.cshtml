@model Empresas

@*<nav aria-label="breadcrumb">

    <ol class="breadcrumb">
    
        <li class="breadcrumb-item"><a href="/empresas/empresasindex">empresas</a></li>
        <li class="breadcrumb-item active" aria-current="page">Update</li>

    </ol>

</nav>*@

@*<form action="/empresas/empresasupdate/@Model.IdCliente" method="POST">

    <input name="pessoas.nome"  placeholder="Nome" class="form-control" value="@Model.Nome"/>
    <input name="pessoas.email"  placeholder="Email" class="form-control" value="@Model.Email"/>
    <input name="pessoas.senha"  placeholder="Senha" class="form-control" value="@Model.Senha"/>
    <br/>

    <button class="btn btn-primary">Atualizar</button>

</form>*@

<!DOCTYPE html>
<html>
<head>
    <title>Atualizar Dados da Empresa</title>

    <style>
        /* Estilos para a página */
        body {
            font-family: Arial, sans-serif;
            background-color: #010101cf;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 60vh;
        }

        .container {
        background-color: #000000d6;
        display: flex;
        flex-direction: column;
        align-items: center;
        position: relative;
        text-align: center; 
        }

        .form{
        display: flex;
        flex-direction: column;
        background-color: #ffffff;
        border-radius: 20px;
        padding: 40px;
        box-shadow: 10px 10px 40px rgb(0, 0, 0);
        gap: 5px;
        text-align: center;
        }

        h1{
        margin-top: 0px;
        color: rgb(0, 0, 0);
        font-size: 30px;
        }

        .btn {
            display: inline-block;
            padding: 7px 14px;
            text-decoration: none;
            color: #fff;
            border-radius: 4px;
            transition: background-color 0.3s;
            margin-left: 10px;
        }

        .btn-primary {
            background-color: #007bff;
            border: 1px solid #000000;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .btn-danger {
            background-color: #dc3545;
            border: 1px solid #000000;
        }

        .btn-danger:hover {
            background-color: #c82333;
        }

        button{
            display: inline-block;
            padding: 7px 14px;
            text-decoration: none;
            color: #000000;
            border-radius: 4px;
            transition: background-color 0.3s;
            margin-left: 10px;
        }

        button{
            background-color: #007bff;
            border: 1px solid #000000;
        }

        input{
        font-size: 20px;
        margin-bottom: 15px;
        display: flex;
        align-items: center;
        position: relative;
        text-align: center;
        box-shadow: 0 6px 4px rgba(0, 0, 0, 0.746);

        flex: 2;
        padding: 8px;
        border-radius: 5px;
        border: 1px solid #ccc;
        width: 100%;
        }

    </style>
</head>

<body>
    <form action="/empresas/empresasupdate/@Model.IdEmpresa" class="form" method="POST">

        <h1>Atualizar dados do Cliente</h1>

        <input name="pessoas.nome"  placeholder="Nome" class="form-control" value="@Model.Nome"/>
        <input name="pessoas.email"  placeholder="Email" class="form-control" value="@Model.Email"/>
        <input name="pessoas.senha"  placeholder="Senha" class="form-control" value="@Model.Senha"/>
        <br/>
    
        <button class="btn btn-primary">Atualizar</button>
    
    </form>
</body>